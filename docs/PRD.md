# PRD: YouTube Comment Analyzer

## 목표
YouTube 영상 URL만 입력하면 댓글을 자동 수집하고 AI로 센티먼트 분석하여, 크리에이터가 시청자 반응을 한눈에 파악할 수 있는 리포트를 생성한다.

## 사용자
YouTube 크리에이터 및 콘텐츠 제작자. 자신의 영상에 대한 시청자 피드백을 빠르게 파악하고 싶은 사람.

## 사전 준비
사용자는 다음 두 가지 API 키를 직접 발급받아야 한다:
1. **YouTube Data API v3 키**: [Google Cloud Console](https://console.cloud.google.com/)에서 프로젝트 생성 → YouTube Data API v3 활성화 → API 키 발급. 일일 무료 할당량 10,000 유닛 (commentThreads.list 1회 = 1 유닛).
2. **Anthropic API 키**: [Anthropic Console](https://console.anthropic.com/)에서 계정 생성 → API 키 발급. 사용량 기반 과금.

---

## 핵심 기능

### 기능 1: YouTube URL 입력 및 댓글 수집
YouTube 영상 URL을 입력하면 해당 영상의 댓글을 자동으로 수집한다.

**지원하는 URL 형식:**
- `https://www.youtube.com/watch?v=VIDEO_ID`
- `https://youtu.be/VIDEO_ID`
- `https://www.youtube.com/embed/VIDEO_ID`
- `https://www.youtube.com/v/VIDEO_ID`
- 위 형식에 추가 쿼리 파라미터(`&t=`, `&list=` 등)가 붙어도 동작

**수집 규칙:**
- 최대 100개 댓글 수집 (YouTube API `maxResults` 상한)
- 관련도순(`order=relevance`) 정렬 — 좋아요 많고 답글 많은 의미 있는 댓글 우선
- 최상위 댓글만 수집 (대댓글 제외)
- 일반 텍스트(`textFormat=plainText`) 형식으로 가져옴

**수용 기준:**
- 유효한 YouTube URL 입력 시 댓글이 수집되어야 한다
- 댓글 수집 중 로딩 상태가 UI에 표시되어야 한다
- 수집된 댓글 수가 리포트에 표시되어야 한다

### 기능 2: AI 센티먼트 분석
수집된 댓글을 Claude AI에 전송하여 센티먼트를 분석한다.

**분석 항목:**
- 전체 요약 (1-2문장)
- 센티먼트 비율: 긍정/중립/부정 퍼센트 (합계 100%)
- 잘하고 있는 점: 최대 5개, 구체적이고 댓글에 근거한 내용
- 개선할 점: 최대 5개, 구체적이고 댓글에 근거한 내용
- 대표 댓글: 긍정/중립/부정 각 2-3개, 원문 인용

**분석 언어:**
- 댓글이 한국어면 분석도 한국어로, 영어면 영어로. 댓글의 주 언어를 따라간다.

**수용 기준:**
- 분석 결과가 JSON 형식으로 파싱 가능해야 한다
- 센티먼트 비율의 합계가 100이어야 한다
- 대표 댓글은 실제 수집된 댓글에서만 인용해야 한다
- 분석 중 로딩 상태가 UI에 표시되어야 한다

### 기능 3: 리포트 표시
분석 결과를 시각적으로 보기 좋은 리포트로 렌더링한다.

**리포트 구성 (위→아래 순서):**
1. 전체 요약 텍스트
2. 센티먼트 비율 가로 바 차트 (긍정=녹색, 중립=회색, 부정=빨강)
3. "잘하고 있는 점" 카드 (불릿 리스트)
4. "개선할 점" 카드 (불릿 리스트)
5. "대표 댓글" 섹션 (센티먼트별 그룹)

**수용 기준:**
- 리포트가 화면에 렌더링되어야 한다
- "다른 영상 분석하기" 버튼으로 초기 상태로 돌아갈 수 있어야 한다

### 기능 4: API 키 관리
사용자가 API 키를 입력하고 브라우저에 저장한다.

**동작:**
- 앱 최초 진입 시 API 키가 없으면 설정 패널이 자동으로 열린다
- API 키는 localStorage에 저장하여 재방문 시 유지한다
- 설정 패널은 접었다 펼 수 있다
- 저장된 키 옆에 체크마크를 표시한다
- 키 입력 필드는 password 타입으로 마스킹한다

**수용 기준:**
- API 키를 입력하고 저장하면 localStorage에 저장되어야 한다
- 페이지를 새로고침해도 키가 유지되어야 한다
- 키가 없는 상태에서 분석 버튼을 누르면 에러 메시지가 표시되어야 한다

---

## 사용자 시나리오

### 시나리오 1: 정상 흐름 (첫 사용)
1. 사용자가 앱에 접속한다
2. API 키가 없으므로 설정 패널이 자동으로 열린다
3. YouTube API 키와 Anthropic API 키를 입력하고 저장한다
4. YouTube 영상 URL을 입력하고 "분석" 버튼을 클릭한다
5. 로딩 상태가 3단계로 표시된다 (댓글 수집 → 분석 중 → 리포트 생성)
6. 리포트가 표시된다
7. "다른 영상 분석하기" 버튼을 클릭하면 URL 입력으로 돌아간다

### 시나리오 2: 정상 흐름 (재방문)
1. 사용자가 앱에 재접속한다
2. localStorage에서 API 키를 자동으로 불러온다
3. 설정 패널은 접힌 상태로 시작한다
4. YouTube URL을 입력하고 바로 분석을 시작한다

### 시나리오 3: 잘못된 URL 입력
1. 사용자가 YouTube가 아닌 URL 또는 잘못된 형식을 입력한다
2. "분석" 버튼 클릭 시 "유효한 YouTube URL을 입력해 주세요" 인라인 에러 표시
3. API 호출은 발생하지 않는다

### 시나리오 4: 댓글 비활성화 영상
1. 사용자가 댓글이 꺼진 영상의 URL을 입력한다
2. YouTube API가 403 에러를 반환한다
3. "이 영상은 댓글이 비활성화되어 있습니다" 에러 표시

### 시나리오 5: 댓글 0개 영상
1. 사용자가 댓글이 없는 영상의 URL을 입력한다
2. 댓글 수집 결과가 빈 배열이다
3. Claude 분석을 호출하지 않고 "이 영상에 댓글이 없습니다" 에러 표시

### 시나리오 6: API 키 오류
1. 사용자가 잘못된 YouTube API 키를 입력한 상태에서 분석을 시작한다
2. YouTube API가 400 에러를 반환한다
3. "YouTube API 키가 유효하지 않습니다. 키를 확인해 주세요." 에러 표시

### 시나리오 7: API 할당량 초과
1. YouTube API 일일 할당량을 모두 소진한 상태에서 분석을 시작한다
2. YouTube API가 403 + `quotaExceeded` 에러를 반환한다
3. "YouTube API 일일 할당량을 초과했습니다. 내일 다시 시도해 주세요." 에러 표시

### 시나리오 8: 비공개/삭제된 영상
1. 사용자가 비공개이거나 삭제된 영상의 URL을 입력한다
2. YouTube API가 404 또는 빈 결과를 반환한다
3. "영상을 찾을 수 없습니다. URL을 확인해 주세요." 에러 표시

### 시나리오 9: 네트워크 오류
1. 분석 도중 네트워크 연결이 끊긴다
2. fetch가 실패한다
3. "네트워크 오류가 발생했습니다. 연결을 확인하고 다시 시도해 주세요." 에러 표시

### 시나리오 10: Claude API 응답 파싱 실패
1. Claude가 유효하지 않은 JSON을 반환한다
2. JSON.parse가 실패한다
3. "분석 결과를 처리하지 못했습니다. 다시 시도해 주세요." 에러 표시

---

## 에러 메시지 매핑

| 원인 | HTTP 상태 | 사용자에게 표시할 메시지 |
|------|-----------|--------------------------|
| 유효하지 않은 YouTube URL | (클라이언트 검증) | 유효한 YouTube URL을 입력해 주세요. |
| API 키 미설정 | (클라이언트 검증) | API 키를 먼저 설정해 주세요. |
| YouTube API 키 무효 | 400 | YouTube API 키가 유효하지 않습니다. 키를 확인해 주세요. |
| YouTube API 할당량 초과 | 403 (quotaExceeded) | YouTube API 일일 할당량을 초과했습니다. 내일 다시 시도해 주세요. |
| 댓글 비활성화 | 403 (commentsDisabled) | 이 영상은 댓글이 비활성화되어 있습니다. |
| 영상 없음/비공개 | 404 | 영상을 찾을 수 없습니다. URL을 확인해 주세요. |
| 댓글 0개 | 200 (빈 배열) | 이 영상에 댓글이 없습니다. |
| Anthropic API 키 무효 | 401 | Anthropic API 키가 유효하지 않습니다. 키를 확인해 주세요. |
| Anthropic API 잔액 부족 | 400 (insufficient_quota) | Anthropic API 잔액이 부족합니다. 크레딧을 확인해 주세요. |
| Anthropic 레이트 리밋 | 429 | 요청이 너무 많습니다. 잠시 후 다시 시도해 주세요. |
| Anthropic 서버 오류 | 500/529 | AI 서비스에 일시적인 문제가 있습니다. 잠시 후 다시 시도해 주세요. |
| Claude JSON 파싱 실패 | (서버 내부) | 분석 결과를 처리하지 못했습니다. 다시 시도해 주세요. |
| 네트워크 오류 | (fetch 실패) | 네트워크 오류가 발생했습니다. 연결을 확인하고 다시 시도해 주세요. |
| 알 수 없는 오류 | 기타 | 예기치 않은 오류가 발생했습니다. 다시 시도해 주세요. |

---

## 제약사항
- 댓글 수집 상한: 100개 (YouTube API `maxResults` 최대값이 100이며, 비용과 분석 품질의 균형점)
- Claude 컨텍스트: 100개 댓글은 약 10,000~30,000 토큰. Sonnet의 입력 한도(200K) 내에서 충분히 처리 가능
- Claude 응답 제한: `max_tokens: 2048` (리포트 JSON에 충분한 크기)
- YouTube API 일일 할당량: 10,000 유닛 (무료 티어). 1회 분석 = 1 유닛이므로 하루 최대 ~10,000회 분석 가능
- localStorage 용량: API 키 2개 저장에 ~200바이트. 브라우저 localStorage 한도(5MB)와 무관

## MVP 제외 사항
- 로그인/회원가입 기능
- 데이터베이스 및 분석 히스토리 저장
- 다국어 UI (분석 결과는 댓글 언어에 맞춰 자동 대응하지만, UI 텍스트는 한국어 고정)
- 여러 영상 비교 분석
- 댓글 시간대별 추이 분석
- 대댓글 수집 및 분석
- 댓글 페이지네이션 (100개 초과 수집)
- 분석 결과 내보내기 (PDF, CSV 등)
- 실시간 스트리밍 응답 (Claude 응답을 한 번에 수신)

## 디자인
- 다크모드 고정, 미니멀
- 무채색 기반 + 센티먼트 표시용 시맨틱 색상 (녹색=긍정, 빨강=부정, 회색=중립)
- 단일 페이지 구성 (입력 → 로딩 → 리포트 상태 전환)
- 반응형 불필요 (데스크탑 우선, 모바일 대응은 MVP 이후)
